<UserControl
    x:Class="LightBulb.Views.Dialogs.SettingsView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:LightBulb.Converters"
    xmlns:dialogs="clr-namespace:LightBulb.ViewModels.Dialogs"
    xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
    MinWidth="350"
    Height="270"
    x:DataType="dialogs:SettingsViewModel">
    <Grid ColumnDefinitions="Auto,*">
        <!--  Settings tab selection  -->
        <Border
            Grid.Column="0"
            VerticalAlignment="Stretch"
            Background="{DynamicResource MaterialDarkBackgroundBrush}">
            <ItemsControl ItemsSource="{Binding Tabs}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,2,0" Classes.active="{Binding IsActive}">
                            <Border.Styles>
                                <Style Selector="Border.active">
                                    <Setter Property="BorderBrush" Value="{DynamicResource MaterialSecondaryMidBrush}" />
                                </Style>
                            </Border.Styles>
                            <Button
                                Margin="4"
                                Padding="8"
                                Command="{Binding $parent[UserControl].((dialogs:SettingsViewModel)DataContext).ActivateTabCommand}"
                                CommandParameter="{Binding}"
                                Foreground="{DynamicResource MaterialDarkForegroundBrush}"
                                Theme="{DynamicResource MaterialFlatButton}"
                                ToolTip.Tip="{Binding DisplayName}">
                                <materialIcons:MaterialIcon
                                    Width="24"
                                    Height="24"
                                    Kind="{Binding Converter={x:Static converters:SettingsTabToMaterialIconKindConverter.Instance}}" />
                            </Button>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <Grid
            Grid.Column="1"
            Background="{DynamicResource MaterialCardBackgroundBrush}"
            RowDefinitions="*,Auto,Auto">
            <!--  Active settings tab  -->
            <ScrollViewer
                Grid.Row="0"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <ContentControl Margin="0,0,8,16" Content="{Binding ActiveTab}" />
            </ScrollViewer>

            <!--  Separator  -->
            <Rectangle
                Grid.Row="1"
                Height="1"
                HorizontalAlignment="Stretch"
                Fill="{DynamicResource MaterialDividerBrush}" />

            <!--  Buttons  -->
            <UniformGrid Grid.Row="2" Columns="3">
                <!--  Reset  -->
                <Button
                    Margin="8"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding ResetCommand}"
                    Theme="{DynamicResource MaterialFlatButton}"
                    ToolTip.Tip="{Binding LocalizationManager.ResetTooltip}">
                    <TextBlock
                        Text="{Binding LocalizationManager.ResetButton}"
                        TextAlignment="Center"
                        TextTrimming="CharacterEllipsis" />
                </Button>

                <!--  Cancel  -->
                <Button
                    Margin="8"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding CancelCommand}"
                    Theme="{DynamicResource MaterialFlatButton}">
                    <TextBlock
                        Text="{Binding LocalizationManager.CancelButton}"
                        TextAlignment="Center"
                        TextTrimming="CharacterEllipsis" />
                </Button>

                <!--  Save  -->
                <Button
                    Margin="8"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding SaveCommand}"
                    Theme="{DynamicResource MaterialFlatButton}">
                    <TextBlock
                        Text="{Binding LocalizationManager.SaveButton}"
                        TextAlignment="Center"
                        TextTrimming="CharacterEllipsis" />
                </Button>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>